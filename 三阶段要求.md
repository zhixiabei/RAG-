# 第三阶段：从单文件 RAG 扩展到多文件 RAG

完成 Naive Chunking 总结以及不同 Chunking Strategy 的简要对比后，下一阶段不进入数据库查询，而是将当前系统从：

**Single-Document RAG**

扩展为：

**Multi-Document RAG**

这一阶段的核心目标是让系统不再只针对用户上传的某一个文件进行问答，而是能够面对一组文档，判断信息来自哪个文件、检索相关内容，并在必要时综合多个文件中的信息进行回答。

---

## 一、当前系统与目标系统的区别

当前系统可以理解为：

```text
Single Document
      ↓
   Chunking
      ↓
  Embedding
      ↓
Vector Store
      ↓
User Question
      ↓
  Retrieval
      ↓
     LLM
      ↓
    Answer
```

下一阶段需要扩展为：

```text
Document A
Document B
Document C
Document D
    ↓
Document Processing
    ↓
Chunking
    ↓
Embedding
    ↓
Vector Store
    ↓
User Question
    ↓
Multi-Document Retrieval
    ↓
Relevant Documents + Relevant Chunks
    ↓
LLM
    ↓
Answer
```

重点不只是“把更多文件放进同一个 Vector Database”，而是要考虑：

> 当知识库中存在很多文件时，系统应该如何找到正确的文件和正确的内容？

---

# 二、多文件数据组织

首先选择一组具有一定关联性的文档作为测试数据。

建议初期控制在：

**10～30 个文件左右**

不需要一开始构建非常大的知识库。

文件之间最好同时存在：

- 内容完全不同的文件
- 同一主题的不同文件
- 存在部分重复信息的文件
- 需要跨文件综合才能回答的问题

例如可以构造：

```text
CO2 吞吐技术介绍.pdf

某区块地质情况.pdf

某区块生产情况.pdf

某井作业总结.pdf

CO2 吞吐项目总结.pdf

行业标准.pdf
```

目的不是测试文件数量，而是制造不同类型的 Retrieval 场景。

---

# 三、Metadata 设计

多文件 RAG 和单文件 RAG 最大的变化之一，是每一个 Chunk 都不能只有文本内容。

需要为 Chunk 增加 Metadata。

例如：

```text
chunk_id

document_id

document_name

document_type

page_number

section

chunk_index

content
```

如果文件本身具有业务属性，也可以加入：

```text
block

well_id

year

category
```

但第一阶段不要求设计复杂 Metadata Schema。

需要理解：

> Chunk 不仅是一段 Text，它还应该知道自己来自哪个 Document、哪个位置。

---

# 四、Multi-Document Retrieval

首先实现最简单的 Baseline：

将所有文件统一：

```text
Documents
    ↓
Chunks
    ↓
Embedding
    ↓
One Vector Store
```

用户 Query 到来以后，直接在所有 Chunk 中进行 Top-K Retrieval。

例如：

```text
User Question
     ↓
Query Embedding
     ↓
Search All Chunks
     ↓
Top-K Chunks
     ↓
LLM
```

这一版本作为：

**Multi-Document RAG Baseline**

先验证系统能够正常工作。

---

# 五、Document-Level Retrieval

完成最基础版本以后，需要开始思考：

如果未来有：

```text
10 documents
100 documents
1,000 documents
10,000 documents
```

是不是每一次 Query 都应该直接搜索所有 Chunk？

因此可以进一步探索：

**Document Retrieval → Chunk Retrieval**

两层检索结构。

例如：

```text
                User Query
                     ↓
              Document Retrieval
                     ↓
              Relevant Documents
                     ↓
               Chunk Retrieval
                     ↓
               Relevant Chunks
                     ↓
                    LLM
```

第一层先判断：

> 哪几个文件最可能与问题有关？

第二层再在这些文件内部寻找：

> 哪些 Chunk 最相关？

可以先实现一个简单版本，例如：

每个 Document 生成：

- Document Summary
- Document Embedding
- Document Metadata

首先针对 Document 做 Top-K Retrieval。

然后只在 Top-N Documents 内搜索 Chunk。

---

# 六、需要重点测试的 Question Types

新的 Evaluation Dataset 不应该只使用之前单文件阶段的问题。

需要增加几种新的问题类型。

## Type 1：Single-document Question

答案完全存在于一个文件中。

例如：

> 某井 CO2 注入量是多少？

主要测试系统能否在多个文件中找到正确文件。

---

## Type 2：Document Identification

问题本身需要系统判断信息属于哪个文件。

例如：

> 哪份报告讨论了 CO2 吞吐后的井间连通性？

测试：

**Document Retrieval 能力。**

---

## Type 3：Cross-document Question

答案需要综合两个或多个文件。

例如：

> 根据地质报告和生产报告，这口井是否适合作为下一轮 CO2 吞吐候选井？

可能需要：

```text
地质文件
+
生产文件
+
作业文件
```

共同回答。

这一类是 Multi-Document RAG 最重要的能力之一。

---

## Type 4：Conflicting Information

不同文件中存在不同版本的信息。

例如：

```text
2024 年报告：孔隙度 18%

2025 年报告：孔隙度 20%
```

观察模型是否能够：

- 识别信息冲突
- 判断来源
- 给出引用
- 避免直接混合成一个答案

---

## Type 5：Unanswerable Question

知识库中不存在答案的问题。

观察系统是否会产生 Hallucination。

---

# 七、Source Attribution

Multi-Document RAG 中必须增加一个要求：

**最终回答需要能够说明信息来源。**

例如：

```text
结论：

该井最近一次 CO2 吞吐后增油效果明显。

依据：

- 《XX井生产报告》第 12 页
- 《XX区块 CO2 吞吐总结》第 8 页
```

或者至少返回：

```text
document_name
page_number
chunk_id
```

这一功能对后续实际企业应用非常重要。

因为多文件以后：

> “答案是什么”

和

> “答案来自哪里”

同样重要。

---

# 八、Evaluation

在现有 Evaluation 基础上增加针对 Multi-Document Retrieval 的评价。

可以考虑：

### Document Recall@K

正确答案所在的 Document 是否进入 Top-K Documents。

### Chunk Recall@K

正确 Chunk 是否进入最终 Retrieval Results。

### Source Accuracy

最终回答引用的 Source 是否正确。

### Answer Accuracy

最终答案是否正确。

### Faithfulness

回答是否完全基于 Retrieved Context。

---

# 九、Baseline 与优化方向

建议按照逐步增加复杂度的方式进行实验。

第一版：

```text
All Documents
      ↓
All Chunks
      ↓
Vector Retrieval
      ↓
Top-K
```

第二版：

```text
Document Retrieval
      ↓
Top-N Documents
      ↓
Chunk Retrieval
```

第三版可以再考虑：

```text
Document Retrieval
      ↓
Hybrid Search
      ↓
Chunk Retrieval
      ↓
Reranker
```

不要求这一阶段全部完成。

重点首先建立一个清晰的：

**Multi-Document RAG Baseline。**

---

# 十、需要重点思考的问题

完成这一阶段以后，需要在报告中回答：

1. 从单文件扩展到多文件以后，Retrieval Accuracy 是否下降？

2. 文件数量增加以后，Top-K 应该如何设置？

3. 是否出现大量来自错误 Document 的 Chunk？

4. Metadata 是否能够帮助 Retrieval？

5. 是否有必要先进行 Document-Level Retrieval？

6. Document Summary 是否能够用于第一阶段筛选？

7. Cross-document Question 为什么比 Single-document Question 更难？

8. 当多个文件信息冲突时，系统应该如何处理？

9. 最终回答是否应该强制保留 Source Citation？

10. 当文件数量继续扩大时，目前的系统架构可能出现什么问题？

---

# 十一、这一阶段最终目标

最终希望形成这样一条演进路线：

```text
Stage 1

Single File
    ↓
Naive Chunking
    ↓
Vector Retrieval
```

```text
Stage 2

Single File
    ↓
Different Chunking Strategies
    ↓
Evaluation
```

```text
Stage 3

Multiple Documents
    ↓
Metadata
    ↓
Document Retrieval
    ↓
Chunk Retrieval
    ↓
Cross-Document Answering
    ↓
Source Attribution
```

再往后的阶段才可以进一步考虑：

```text
Documents
Database
API
Internal Systems
      ↓
Unified Retrieval / Tools
      ↓
Agent Runtime
```

因此当前阶段先重点解决：

> **当知识从“一个文件”扩展到“一组文件”以后，系统如何正确找到信息、组织信息，并告诉用户这些信息来自哪里。**
