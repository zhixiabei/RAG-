# RAG 项目下一阶段任务要求

本阶段工作分为三个部分：

1. 对上一阶段 **Naive Chunking 文件级 RAG 项目进行总结和汇报**
2. 在现有评估体系下，**简要探索不同 Chunking Strategy**
3. 在完成单文件级 RAG 阶段性总结后，开始进行多文件级 RAG 的设计与实现

整体要求不是单纯增加模型或算法复杂度，而是逐步建立对 **RAG 系统结构、检索效果、失败原因和后续扩展方向** 的完整认识。

---

## 一、Naive Chunking 文件级 RAG 项目总结报告

首先对目前已经完成的文件级 RAG 项目形成一份完整的项目报告。

报告需要做到：**其他人即使没有参与过项目，也能够通过这份报告理解整个系统是如何工作的、效果如何、目前的问题是什么。**

### 1. 项目背景与目标

简要说明：

- 为什么要做这个 RAG 系统
- 当前系统解决什么问题
- 当前支持什么类型的数据/文件
- 用户输入是什么
- 系统最终输出是什么
- 当前阶段采用了什么技术路线

不需要写过多行业背景，重点描述目前项目自身。

---

## 二、系统整体架构

这一部分是报告重点。

需要画出一张完整的 **系统架构框图 / Workflow Diagram**。

要求能够清晰展示整个 RAG 系统由哪些部分组成，以及不同部分之间如何配合。

例如可以抽象为：

```text
Document
   ↓
Document Parsing
   ↓
Chunking
   ↓
Embedding
   ↓
Vector Database
   ↓

User Query
   ↓
Query Embedding
   ↓
Retriever
   ↓
Top-K Chunks
   ↓
Prompt Construction
   ↓
LLM
   ↓
Final Answer
```

实际框图需要根据当前项目真实代码结构进行绘制，不要求完全按照上述结构。

对于框图中的每个模块，需要分别说明：

- 该模块的作用
- 输入是什么
- 输出是什么
- 使用了什么模型 / 库 / 方法
- 与上下游模块如何交互

例如：

### Document Parsing

说明当前文件是如何读取和解析的。

### Chunking

说明：

- 当前采用的 Naive Chunking 方法
- Chunk Size
- Overlap
- 是否按 token / character / paragraph 进行切分
- 为什么最初采用这种方法

### Embedding

说明：

- 使用的 Embedding Model
- 输入输出形式
- Embedding Dimension
- 如何写入向量数据库

### Retrieval

说明：

- 使用什么相似度
- Top-K 如何设置
- 返回什么内容

### Generation

说明：

- 如何构建 Prompt
- Context 如何插入
- 使用什么 LLM
- 最终回答如何生成

如果代码本身已经进行了模块化，也需要尽量让：

**代码结构 ↔ 系统框图**

能够对应起来。

---

# 三、项目效果展示

这一部分要求同时包含：

**实际效果截图 + 量化分析 + 质性分析。**

不能只给几个回答截图，也不能只给最终平均分。

## 1. 实际效果截图

选择若干具有代表性的测试案例进行展示。

建议至少包括：

- 一个效果较好的简单问题
- 一个需要跨多个 Chunk 获取信息的问题
- 一个检索结果不理想的问题
- 一个最终回答错误或不完整的问题

截图中尽量能够展示：

```text
用户问题
↓
Retrieved Chunks / Context
↓
模型最终回答
```

目的是能够看到：

> 系统为什么得到了这个答案。

而不仅仅展示最终回答。

---

# 四、量化分析

继续使用上一阶段已经建立的 Evaluation 方法，对系统效果进行汇总。

需要明确：

- Evaluation Dataset 有多少条问题
- 问题是什么类型
- Ground Truth 如何获得
- 使用了哪些指标
- 每个指标分别衡量什么

然后给出当前 Naive Chunking 系统的结果。

例如可以包括：

```text
Retrieval Recall@K

Retrieval Precision

Answer Accuracy

Faithfulness

Context Relevance

Latency

Token Consumption
```

具体指标以当前实际已经实现的 Evaluation 为准，不要求为了报告额外堆很多指标。

重点是解释：

> 每一个指标到底代表什么。

以及：

> 当前系统在哪些指标上表现较好，哪些指标存在明显问题。

---

# 五、质性分析

除了平均分之外，需要人工查看部分案例，对系统失败情况进行分类。

重点分析：

### Retrieval Failure

例如：

- 正确内容没有被检索到
- 正确内容存在，但排名较低
- Chunk 太大导致噪声过多
- Chunk 太小导致信息不完整

### Generation Failure

例如：

- Context 是正确的，但 LLM 理解错误
- 模型遗漏部分 Context
- 模型进行了无依据推断
- 回答格式存在问题

### Dataset / Document Problem

例如：

- 原文自身描述不清楚
- 同一信息出现在多个位置
- 文档结构导致切块困难
- Evaluation Ground Truth 存在歧义

最终希望能够初步回答一个问题：

> **当前系统的主要瓶颈到底是在 Retrieval、Chunking、Generation，还是 Evaluation 本身？**

---

# 六、当前阶段思考

在报告最后加入一部分个人思考。

不要求一定得出正确结论，但需要基于实验结果思考，例如：

- Naive Chunking 最大的问题是什么？
- Chunk Size 对检索结果有什么影响？
- Overlap 是否明显改善效果？
- 什么类型的问题最容易失败？
- 当前 Vector Retrieval 有什么不足？
- 是否存在 Chunking 无法解决的问题？
- 如果继续提高系统效果，优先应该优化哪个部分？

重点不是总结已有知识，而是：

> **结合这次自己实际实现和测试的系统进行分析。**

---

# 七、第二阶段：Chunking Strategy 简要探索

完成 Naive RAG 报告以后，在当前系统基础上，对其他 Chunking Strategy 进行一轮简要实验。

这一阶段不要求做深入的算法研究，也不要求实现大量复杂 Chunking 方法。

建议选择 3～4 种具有明显差异的 Strategy。

例如：

### Baseline

**Fixed-size / Naive Chunking**

作为目前 Baseline。

### Structure-aware Chunking

例如：

- Paragraph-based
- Recursive Text Splitter
- Heading / Section-aware Chunking

尽量利用文档自身结构。

### Hierarchical / Parent-Child Chunking

可以探索：

```text
Parent Chunk
    ↓
Child Chunk
    ↓
Embedding & Retrieval
    ↓
返回对应 Parent Context
```

如果实现复杂度较高，可以做简化版本。

### Semantic Chunking

如果时间允许，可以尝试一种 Semantic Chunking 方法。

不要求投入大量时间研究复杂 Agentic Chunking 等方法。

---

# 八、Chunking Strategy 对比实验

所有 Chunking Strategy 尽量使用：

**同一套文档、同一套 Evaluation Dataset、同一个 Embedding Model、同一个 Retriever、同一个 LLM。**

尽量只改变：

> Chunking Strategy

避免同时修改大量变量导致结果无法解释。

最终形成类似下面的对比：

| Strategy | Recall@K | Answer Accuracy | Latency | Token Usage |
|---|---:|---:|---:|---:|
| Naive | | | | |
| Recursive | | | | |
| Structure-aware | | | | |
| Parent-Child | | | | |

除了最终数字，还需要挑选几个 Case Study 分析：

> 为什么某一种 Chunking 在这个问题上明显好于另一种？

这一阶段最终希望回答：

> **Chunking Strategy 对当前文件级 RAG 的实际提升到底有多大？**

